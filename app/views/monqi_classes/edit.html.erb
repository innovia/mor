<h2>Editing Monqi Class</h2>

<%= error_messages_for :monqi_class %>

<% form_for(:monqi_class, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag "Update"%>
  </p>
<% end %>

<br/>

<%= link_to 'Show', object_url %>
|
<%= link_to 'Cancel', collection_url %>