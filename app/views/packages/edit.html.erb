<h2>Editing Package</h2>

<%= error_messages_for :package %>

<% form_for(:package, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "edit_form", :locals => { :f => f } %>
  <p>
    <%=submit_tag "Update"%>
  </p>
<% end %>

<br/>

<%= link_to 'Show', object_url %>
|
<%= link_to 'Back', collection_url %>