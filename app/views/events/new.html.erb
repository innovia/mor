<h2>Schedule a new class</h2>

<%= error_messages_for :event %>
<%= flash[:notice] %>
<% form_for(:event, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create", :class => 'create_button' %>
  </p>
<% end %>
<br/>

<%= link_to 'Back', collection_url %>