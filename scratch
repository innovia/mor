
\\  
  , :partial => "/shared_partials/quick_class_addon"  

# if All Calendars View is selected:
elsif @calendar.is_a?(Array)
      page.replace_html "calendar_name",	"<h2>  All Calendars View  </h2>"
      page.replace_html "calendar_collection", ""
      @calendar.each do |cal|    
        page.insert_html :bottom, "calendar_collection", 
        :partial => "/shared_partials/event_template", :collection => cal.event_templates
      end

      #if calendar is set to Personal Trainning and is not nil (has events on it)
elsif @calendar.id == 2 
      page.replace_html "calendar_name",	"<h2> #{@cal_name} Calendar </h2>"
      page.replace_html "calendar_collection", :partial => "/shared_partials/event_template", :collection => @calendar.event_templates     
      page.insert_html :top, "calendar_collection", :partial => "/shared_partials/quick_class_addon" 
else
        page.replace_html "calendar_name",	"<h2> #{@calendar.name} Calendar  </h2>"
        page.replace_html "calendar_collection", :partial => "/shared_partials/event_template", :collection => @calendar.event_templates  
end


  

quick class